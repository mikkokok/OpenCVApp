<Window x:Class="OpenCVApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OpenCVApp"
        mc:Ignorable="d"
        Title="OpenCV Application" Height="350" Width="525" MinHeight="350" MinWidth="525" Background="#FFB1F3F0">
    <Grid MinWidth="500" MinHeight="300">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition MinWidth="300" Width="*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition MaxHeight="50" MinHeight="50"/>
            </Grid.RowDefinitions>
        </Grid>
        
            <TextBox x:Name="BigTextBlock" Grid.Column="0" HorizontalAlignment="Stretch" TextWrapping="Wrap" Text="{Binding Path=Message}" Background="#FFC2F0BB" VerticalScrollBarVisibility="Auto" IsReadOnly="True" />
        <Button Grid.Column="1" Click="Button_Click">OK</Button>
        <!--Text="Welcome to OpenCV Application"-->
    </Grid>
</Window>
